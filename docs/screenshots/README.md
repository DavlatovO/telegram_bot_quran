# Screenshots

Drop PNG/JPG captures of the bot here so the main README can display them.

Expected files (referenced by ../../README.md):

- `Start.png`        - /start welcome + language picker
- `languages.png`    - language selected confirmation
- `byVerse.png`      - single ayah lookup (e.g. `2:255`)
- `Navigation.png`   - a surah open with Prev/Next buttons
- `byName.png`       - lookup by surah name (e.g. `al-fatiha`)
